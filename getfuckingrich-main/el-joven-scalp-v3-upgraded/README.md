# El Joven Scalp Bot (Pro Version)

Features:

• Multi-exchange price aggregation
• RSI trading signal engine
• WebSocket price streaming
• AI router support
• Rate limiting
• JWT auth ready
• Docker deployment

API:

GET /health
GET /price
GET /signal

Run:

npm install
npm run start

or

docker compose up